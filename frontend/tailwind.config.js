/** @type {import('tailwindcss').Config} */
// Use CommonJS export for maximum compatibility with PostCSS tooling
// This project has removed Tailwind CSS and now uses plain CSS.
// The original tailwind.config.js was left as a reference; the app's styling comes from src/index.css.

module.exports = {};
